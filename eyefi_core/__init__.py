"""eyefi_core: async, framework-agnostic Eye-Fi card upload server.

Zero homeassistant.* imports. Consumed either embedded (imported directly by
custom_components/eyefi) or as a standalone daemon (eyefi_core/service.py).
"""

__version__ = "0.1.0"
